# Local (default if unset): SQLite file life_planner.db
# DATABASE_URL=sqlite:///life_planner.db

# Docker Compose / AWS RDS example (Postgres):
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/life_planner

# Comma-separated frontends, or * for demo
# CORS_ORIGINS=http://localhost:3000,https://app.example.com

# Run alembic upgrade head when the API process starts (useful on App Runner)
# RUN_MIGRATIONS_ON_STARTUP=true
